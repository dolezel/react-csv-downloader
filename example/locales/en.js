import enLocaleData from 'react-intl/lib/locale-data/en';

export const en = {
  locale: enLocaleData,
  messages: {
    foo: {
      default: 'foo',
      bar: {
        default: 'bar',
        full: 'foobar'
      }
    },
    test: {
      default: 'toto {test}'
    }
  }
};
